{
  "schemaVersion": "asset/v1",
  "id": "payloads.sqli",
  "name": "SQL injection payload templates",
  "description": "Dialect-aware SQL injection payload guidance for authorized validation, including read-only proofs and second-order verification patterns.",
  "sources": [
    {
      "path": "assets/payloads/sqli/README.md",
      "mode": "canonical"
    }
  ],
  "format": "markdown",
  "risk": "dual-use",
  "license": "internal",
  "usedBy": ["validate-worker", "poc-gate", "regression-gate"],
  "inputs": ["database dialect", "injection context", "operation class", "safe level"],
  "outputs": ["raw HTTP payload fragment", "assertion strategy", "cleanup guidance"],
  "review": {
    "lastReviewedAt": "2026-05-28",
    "reviewedBy": "gpt-5.5",
    "openQuestions": [
      "Convert canonical markdown guidance into dialect-specific JSON templates after quality gate stabilizes."
    ]
  },
  "regressionCases": ["ems:vul-001", "ems:vul-126", "ems:vul-127", "ems:vul-131"],
  "migrationNotes": [
    "Canonical v2 asset records SQL validation rules without depending on legacy prompt files.",
    "Destructive SQL is not part of default validation; L2 writes require mutation evidence and cleanup."
  ]
}
