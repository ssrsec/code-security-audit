---
description: "安全审计报告格式约束。在生成或编辑 audit/ 下的报告文件时自动激活，强制执行严格 5 章节结构、完整 CVSS 3.1 向量格式、零容忍占位符规则、实战利用最低要求。"
alwaysApply: false
globs: "audit/*.md"
---

# 审计报告格式约束

## 报告结构纪律（严格 5 章节）

报告有且仅有以下五个顶级章节，**不得增删改标题**：

1. **一、项目代码审计总结**
2. **二、漏洞汇总表**
3. **三、漏洞详情**
4. **四、组合漏洞摘要**
5. **五、总体安全建议**

**严禁在第五章节后添加任何内容**（免责声明、附录、报告说明等一律禁止）。

## 章节一必填字段

| 字段 | 说明 | 约束 |
|------|------|------|
| 覆盖率 | 已审 X / 应审 Y = 100% | 必填 |
| 审计目标 | 项目名称、路径 | 必填 |
| 开始时间 | 从 `audit/phase0/metrics.md` 读取 | 必填，不得编造 |
| 结束时间 | 执行 `date "+%Y.%m.%d %H:%M:%S"` 获取 | 必填，不得编造 |
| 技术栈 | 语言、框架、中间件、数据库 | 必填 |
| 发现总结 | 各等级数量 + 组合漏洞数量 + 类型分布 + 触发条件统计 | 必填 |

## 每条漏洞必填字段（表格格式）

```markdown
| 字段 | 内容 |
|------|------|
| 漏洞编号 | vul-XXX |
| 漏洞名称 | 标准漏洞类型开头+括号补充关键条件（如"SQL 注入（orderBy 参数拼接）"），禁止用代码类名/方法名/HTTP 路径命名 |
| 漏洞描述 | 简要说明漏洞原理和危害 |
| 漏洞等级 | 严重 / 高危 / 中危 / 低危 |
| 验证状态 | 已确认 / 待验证 |
| CVSS评分 | 完整向量字符串（见下方格式要求） |
| 前置条件 | 具体前置条件（术语全报告统一） |
| 访问权限 | 明确所需权限（不得用"视具体接口"等敷衍描述） |
| 调用链 | 每跳标注文件:行号（href 中不加 #L行号） |
```

「待验证」漏洞**必须额外增加「待验证内容」行**，逐条列出需运行时验证的具体事项。

每条漏洞必须包含三个独立章节：**【复现步骤】**、**【实战利用】**、**【修复建议】**。

**【修复建议】要求**：
- 指明具体文件、函数或配置项 + 代码示例 + 修复原理 + 回归测试建议
- **必须评估修复方案对业务的影响**（修复是否影响正常业务功能？如会影响，提供兼顾安全和业务的方案）
- 提供分级修复建议：① 紧急临时方案（快速止血）+ ② 长期根本方案（彻底修复）
- 如修复涉及架构级变更，标注"需评估业务影响后执行"

## CVSS 3.1 评分强制格式

```
CVSS:3.1/AV:X/AC:X/PR:X/UI:X/S:X/C:X/I:X/A:X → X.X
```

- **严禁使用「约」「大约」「≈」等模糊词**，每个指标必须明确选定一个枚举值并附简要理由
- 存在多种触发场景时，分别列出两个完整向量字符串及对应分数

## 复现步骤零容忍占位符（一律违规，退回重写）

以下任何形式均为违规：

| 违规类型 | 示例 |
|---------|------|
| 通用占位 | `YOUR_HOST`、`REPLACE_XXX`、`REDACTED_JWT` |
| REPLACE 式 | `REPLACE_WITH_VALID_TOKEN`、`<FILL_IN>` |
| 注释占位 | `<!-- 此处替换为... -->`、`<!-- payload here -->` |
| 延迟话术 | `此处从略`、`细节略`、`不再展开`、`需按目标编写` |
| 空壳标签 | `<root/>`、`<payload/>`、`TODO` |
| Python 省略 | `...`（省略号）、`pass`、`# TODO` |

**正确做法**：
- Host → `127.0.0.1:端口`（从 `server.port` 等配置文件读取端口）
- 认证令牌 → `{{access-token}}`，前置步骤写明获取方式（如 `POST /api/login` + 默认账密）
- 业务 ID → 前置步骤说明获取接口，数据包中用示例格式值（UUID：`550e8400-...`）
- 反序列化 payload → 必须是与项目 classpath 匹配的具体 gadget 链，不得用 `<root/>`

## 实战利用最低要求（硬性标准）

**严禁一句话带过**（如"可读取 /etc/passwd"），场景数量按漏洞复杂度分级（与 `shared/report_fields.md` 保持一致）：

- **复杂漏洞**（注入/反序列化/SSRF/组合利用）：至少 **2 个**含完整 payload 的场景
- **简单漏洞**（硬编码凭证/默认密码/未授权访问/信息泄露）：**1 个**详细利用场景即可
- **待验证漏洞**：至少 **1 个**假设验证通过后的场景，标注条件假设

| 漏洞类型 | 最低场景数 | 示例 |
|---------|-----------|------|
| 反序列化 | 2 | 具体 gadget 链 payload + 第二种利用方式 |
| SQL/HQL 注入 | 2 | UNION/报错注入 + 时间盲注 + 自动化脚本 |
| 文件读取/SSRF | 2 | 读取高价值文件 + 读取另一类目标 |
| 硬编码凭证/默认密码 | 1 | 利用凭证登录并获取敏感数据 |
| 未授权访问/信息泄露 | 1 | 敏感数据获取数据包 |

**检测规则**：场景中不包含任何 HTTP 数据包、代码块或具体 payload 字符串 → 视为不合格，退回重写。

## 「待验证」漏洞同等重要

- **禁止丢弃**代码层面高度确认的漏洞
- **禁止降级**为"说明段落"放在摘要中（如"Fastjson 反序列化说明（未单列 vul 的原因）"）
- 待验证漏洞的复现步骤：给出最佳努力 payload，标注哪些部分需运行时调整
- 待验证漏洞的实战利用：给出假设验证通过后的场景（可标注条件假设）
