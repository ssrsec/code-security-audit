---
description: "代码审计反幻觉与验证原则。在 audit/ 目录下工作时自动激活，要求所有文件路径必须工具验证存在、代码片段仅来自 Read 输出、调用链每跳标注行号、不确定标「待验证」而非丢弃。"
alwaysApply: false
globs: "audit/**"
---

# 反幻觉铁律（代码审计专用）

## 5 条硬约束

### 1. 报告漏洞前必须用工具验证文件存在
- 将任何文件路径或代码位置写入报告前，**必须先 Glob 或 Read 确认文件存在且可读**
- 不得根据记忆或推测写出路径

### 2. 代码片段必须来自实际 Read 输出
- 报告中引用的所有代码片段必须来自 Read 操作结果
- 不得凭印象改写、补全或生成代码
- Read 时优先只读相关行区间，避免整文件 Read

### 3. 调用链每一跳必须标注文件:行号
- 数据流/调用链每一跳必须标注 **文件路径:行号**
- 格式示例：`UserController.java:45 → UserService.java:12 → UserDao.java:33`
- 某一跳无法确定行号则标"待确认"，整条链标为**待验证**

### 4. 不确定的发现标「待验证」不得丢弃
- **已确认**：完整验证可利用，PoC 可执行或调用链完整
- **待验证**：发现可疑但无法完全确认（调用链断裂、过滤有效性不确定、版本不明等）
- 宁可标为待验证交由人工验证，不得将不确定的标为已确认
- **不确定 ≠ 丢弃**：证据不足以标「已确认」时必须标「待验证」并详细注明待验证内容

### 5. 禁止虚报目录内容不存在
- **严禁在未执行 Glob/find 的情况下声称某目录"不存在"或"为空"**
- 发现 `.class`/`.jar` 编译产物时，必须按反编译流程处理，不得以"无法审计"为由跳过

## 「待验证」漏洞必须进入报告

以下场景**必须**标「待验证」进入报告，**严禁丢弃**：
- 已知漏洞版本的危险 API 调用（Fastjson 1.2.7 `parseObject`、XStream 1.4.8 `fromXML` 无白名单）
- 反序列化 Gadget 链需运行时确认（classpath 有可疑依赖但无法 100% 确认）
- SQL 注入拼接确认但方言需微调
- 前端 `eval()` 解析服务端返回，危害程度待确认

**仅当代码分析明确证明条件不可达时**（如 Sink 前有不可绕过的白名单），才可排除不进入报告。

## 调用链上下文保护

跨 2 个以上文件的数据流追踪，必须将每一跳写入 `audit/phase2/callchain_tracker.md` 再跳转到下一个文件，防止上下文丢失。格式：

```
## com-001: [Sink 类型]
1. 入口: UserController.java:45 — 参数 userId 来自 @RequestParam
2. 中转: UserService.java:102 — 传入 findUser()
3. Sink: UserDao.java:33 — 拼接进 SQL 字符串
状态: 已确认
```
