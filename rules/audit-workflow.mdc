---
description: "代码安全审计工作流协议。当用户说「开始审计」「安全审计」「代码审计」「对 XXX 做安全审计」「继续审计」时自动激活，约束 AI 按 Phase 0→1→2→3→4→5→6 线性执行，禁止跳步、禁止抽样、禁止提前出报告。"
alwaysApply: false
globs:
---

# 代码安全审计工作流约束

## 核心原则

### 只关注有实际危害的漏洞（极致降噪）
- **只报以下类型**：RCE / 文件上传 GetShell / 越权 / 未授权访问 / SQL 注入 / 任意文件读取 / SSRF 有进一步利用 / 反序列化 / SSTI / 弱口令
- **绝对不报（发现即丢弃，不分配编号）**：
  - DoS / 拒绝服务、代码鲁棒性问题（空指针、重启函数）
  - CSRF 防护缺失、SSRF 访问正常内网 Web 服务无进一步利用
  - 注释死代码、无外部可控入口的假设性方法、读取本地环境变量的操作
  - 日志格式（SQL 打印到 StdOut）、Cookie 无 Secure 标记、跨域配置、限流缺失
- **判定标准**：必须同时满足「外部可控攻击路径」+「触发条件可行」+「实际权限/数据危害」

### 例外（绝不能因降噪而丢弃真实漏洞）
已知漏洞版本的危险 API 调用（Fastjson 1.2.7 `parseObject`、XStream 旧版 `fromXML` 无白名单、`eval()` 等）→ **必须作为「待验证」漏洞进入报告**，注明需验证的具体内容。

## 阶段执行（严禁跳步）

必须严格按顺序执行：**Phase 0 → 1 → 2 → 3 → 4 → 5 → 6**

| Phase | 任务 | 关键约束 |
|-------|------|---------|
| 0 | 度量 + 反编译预处理 | 必须用 Glob 扫描 `.class`/`.jar`/`.war`/`.dll`，不得凭猜测跳过 |
| 1 | 项目侦察 | 生成 `audit/phase1/in_scope_files.txt`（覆盖率分母唯一来源） |
| 2 | 全量审计（Sink-driven + Control-driven 并行）+ 原语收集 | T1 文件完整 Read，T2/T3 先 Grep 后精准 Read；同时输出 primitives_batch{N}.md |
| 3 | 覆盖率校验 | 覆盖率 = 已审/应审，未达 100% 自动继续下一批 |
| 4 | 漏洞验证 | 代码优先判定，不一律写"需人工验证" |
| 5 | 双轨组合分析 | audit-validate-agent（漏洞组合）+ audit-primchain-agent（原语组合）并行 |
| 6 | 最终报告 | 合并所有漏洞，严格 5 章节输出，完成后清理中间文件 |

## 绝对禁止行为

1. **禁止跳步**：Phase 4 未完成不得生成报告
2. **禁止抽样**：必须 100% 逐文件审阅
3. **禁止多选**：进度未到 100% 时，最后一句只能是「请发送『继续审计』以完成剩余部分」
4. **禁止估算**：进度必须精确计算，格式：「当前进度：已覆盖 X/Y 个文件，精确覆盖率为 Z%」

## 强制进度格式

每批次结束必须输出：
```
审计进度：已审 X / 总计 Y 文件 — Z% 覆盖率
```

未达 100% 时结尾唯一指示：
```
当前覆盖率 Z%，审计尚未完成，请发送『继续审计』进行下一批次扫描。
```

## Phase 6 纪律

- 报告生成并确认无误后，**过程文件默认保留**（用于证据追溯）
- **严禁删除** `audit/decompiled`（反编译输出是审计源代码基础）
- **严禁删除** `audit/phase4`（已验证漏洞证据，法律/合规追溯基础）
- **仅当用户明确要求精简交付包时**，将 phase0/phase1/phase2/phase3/phase5 归档到 `audit/.archive/<timestamp>/`
- 最后一句话有且仅有：「代码安全审计流程已全部完成，最终报告已生成至 audit/security_audit_report.md，中间过程文件已保留（如需精简交付包，请告知）。」
- 之后**禁止**提出任何流程外选项
